# Network-Framework 技术点速览

> 面向没学过网络编程、对 epoll 没有概念的协作者。改了很多版,逻辑有点乱,这份是当前版本的完整梳理。

## 一、一句话定位

一个 **Linux 下的 C++17 网络服务器框架**:主线程用 epoll 管所有连接(前台),重活丢给线程池(后厨),业务逻辑用回调注入(换个回调 = 换个业务)。

## 二、整体架构:前台 / 后厨

```
客户端 ──TCP──▶ ┌────────────────────────────────────────────┐
客户端 ──TCP──▶ │         主线程 = 前台(EpollServer)         │
客户端 ──TCP──▶ │   epoll_wait 盯着所有 socket               │
                │   · 新连接来了  → accept,登记进 epoll       │
                │   · 数据到了    → 派活给线程池(post)        │
                │   · 能发送了    → 派活续发                   │
                │   · 心跳        → 定期扫,踢掉死连接          │
                └──────────────┬──────────────────▲──────────┘
                               │ post 任务        │ 塞发送缓冲
                               ▼                  │
                ┌──────────────┴──────────────────┴──────────┐
                │       线程池 = 后厨(多个 worker 抢任务)      │
                │   recv → 拆包 → 业务回调 → 塞发送缓冲 → 尝试发│
                └────────────────────────────────────────────┘
```

## 三、三个核心概念

**1. epoll —— "来电提醒"**
你有一堆 socket(电话),不知道哪个有数据。epoll 是内核的"总机":你把自己关心的 socket 全交给它,它盯着,谁有动静就叫醒你一次(`epoll_wait` 返回哪个 fd)。不用你挨个拿起听筒试。

**2. Reactor 模式 —— 前台只接单,后厨做菜**
主线程 = 前台,只干三件事:**发现事件 → 派活 → 定时巡检**。真正收数据、拆包、跑业务都是 worker 干的。为什么?网络 IO 不能阻塞主线程——一个 recv 卡住,100 个连接全停摆。

**3. 线程池 —— 一群后厨抢单**
主线程把活 `post` 进任务队列,一群 worker 线程抢着干。多核并行,主线程永不卡。

## 四、一条消息的完整旅程(核心数据流)

```
1. 客户端连接 → 主线程 accept → 建 Connection(档案袋:收/发缓冲)→ 注册 epoll"可读"
2. 客户端发数据 → epoll 报"可读" → 主线程 post 任务到线程池
3. worker:recv 收数据 → 塞进接收缓冲 → 拆包(按长度头切出完整消息)→ 调业务回调
4. 业务回调(回显=原样返回 / 聊天室=广播)→ 把回复塞进发送缓冲
5. worker 尝试发:发完 → 完事;没发完 → 注册 epoll"可写"
6. epoll 报"可写" → 再派活 → worker 续发,直到发完
7. 客户端断开 → recv 返回 0 → 优雅关闭(把剩余数据发完再关)
```

## 五、关键机制

| 机制 | 为什么 |
|---|---|
| **4 字节长度头** | TCP 是字节流,没消息边界。每条消息前 4 字节标长度,拆包按长度切 |
| **粘包 / 半包** | 一次 recv 可能收到多条(粘包)或半条(半包),拆包循环处理 |
| **非阻塞 socket** | recv/send 没货立刻返回 EAGAIN,不卡线程 |
| **EPOLLONESHOT** | 事件触发一次就失效,处理完必须重新注册 → 同一连接同一时刻只有一个任务在处理 |
| **发送三态** | SentAll(发完)/ Pending(没发完,等可写事件)/ Error(出错关闭) |
| **优雅关闭** | 对方断开时先把发送缓冲排空再关,数据不丢 |
| **心跳** | 死连接不产生事件,主线程每 ~1 秒扫一遍,超时踢掉 |
| **最大帧长** | 长度头超过 4MB 判协议错误断开,防恶意半包吃内存 |
| **高水位** | 发送缓冲堆积超 1MB 判慢客户端断开,防内存炸弹 |
| **ConnectionId + generation** | 任务带"令牌";连接关闭后 fd 可能被内核复用给新连接,令牌对不上就作废,防止旧任务误操作新连接 |

## 六、模块地图

```
src/
  net/     EpollServer(前台)+ Connection(连接档案袋)
  thread/  ThreadPool + MessageQueue(任务队列)+ WorkThread
  log/     Logger(异步日志,不阻塞业务)
tests/
  integration/   pytest 集成测试(回显/粘包/半包/并发)
  unit/          独立 C++ 测试
benchmarks/      压测(echo.py,持续压测 + CSV 记录)
```

## 七、当前进度

- **阶段1(资源热点)**:线程池 post()、缓冲区下标消费、最大帧长、accept 循环、epoll 事件完备、慢客户端高水位
- **阶段1b(生命周期安全)**:ConnectionId + generation 代际校验,解决 fd 复用竞态
- **压测**:echo.py 持续压测,QPS 稳定 ~3 万(1000 并发);1000 并发最大延迟 275ms(全局锁竞争,下一步要消)
